export interface LocationInput {
  // Human-readable place name used by the route.
  name: string;

  // Optional coordinate for later integrations.
  latitude?: number;

  // Optional coordinate for later integrations.
  longitude?: number;
}
